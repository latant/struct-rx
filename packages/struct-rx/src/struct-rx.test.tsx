import {} from "./struct-rx";

describe("Struct RX tests", () => {
  it("should manipulate DOM", () => {
    document.body.innerHTML =
      "<div>" + '  <span id="username" />' + '  <button id="button" />' + "</div>";
  });
});
